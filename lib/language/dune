(include_subdirs unqualified)

(library
 (name language)
 (modules typ syntax parser lexer value memory bigStep bigStepUtil derivator visualizer textSvg))

(ocamllex lexer)

(menhir
  (modules parser))
