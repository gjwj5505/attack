(executable
 (public_name attack)
 (name main)
 (libraries
  language
  ; Temporarily disabled during the language-only C subset rewrite.
  ; Re-enable analyzer first once the C language layer is ready.
  ; analyzer
  ; attack_config
  ; synthesis
  ))

(rule
 (deps main.exe)
 (target attack)
 (action
  (copy %{deps} %{target}))
 (mode
  (promote
   (until-clean)
   (into ..))))
