# TODO

## Next: CIL-facing core

- Define the structural core AST that should correspond to Sparrow/CIL CFG
  commands.
- Decide whether the core should be statement/block based or explicit CFG-edge
  based.
- Specify how the current C-like surface syntax maps into the core.
- Update `syntax.ml`, `bigStep.ml`, and `derivator.ml` around that core.
- Keep the current C-like parser/printer as a readable facade if it remains
  useful.

## Semantics

- Keep Big-Step tree generation as the concrete semantics oracle.
- Preserve current no-tree behavior for UB and derivation failures:
  overflow, division/modulo by zero, missing return, memory errors, out of fuel.
- Add future features only after checking CIL lowering and Sparrow
  `IntraCfg.Cmd` representation.
- When adding `break`/`continue`, connect parser syntax to the existing
  `control` constructors.
- When adding side-effect expressions or calls, revisit expression evaluation
  order and memory effects.

## Validation

- Keep `dune build` green after each small step.
- Keep success examples:
  - `examples/small_while.c`
  - `examples/branch_loop.c`
- Keep error examples that must not produce a Big-Step tree:
  - `examples/error_div_zero.c`
  - `examples/error_overflow.c`
  - `examples/error_literal_overflow.c`
  - `examples/error_missing_return.c`
  - `examples/error_unbound.c`
  - `examples/error_duplicate.c`
  - `examples/error_out_of_fuel.c`
- Add one small validation example for every new semantic feature.

## Analyzer / Synthesis reconnect

- Re-enable synthesis only after the core AST and Big-Step semantics stabilize.
- Port size, prune, grow, and component generation rules from the old G
  baseline to the new core.
- Re-enable Sparrow analyzer wrapper after printable C/CIL-facing programs are
  stable.
- Compare generated concrete executions against Sparrow results at the
  CIL/IntraCfg command level.
- Later optimization: avoid blocking on every Sparrow call; run analyzer jobs
  concurrently and consume results as they finish.

## Cleanup

- Keep `.agents/CONTEXT.md` updated with durable design decisions.
- Keep `.agents/LANGUAGE.md` as the language/semantics source of truth.
- Do not update `.agents/c-feature.csv` until we intentionally revisit the
  feature matrix.
