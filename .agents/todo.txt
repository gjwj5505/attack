# TODO

## Next: CIL' Big-Step

- Define CIL' runtime values.
- Define addresses, memory blocks, and variable allocation.
- Define global/function environments.
- Implement lvalue evaluation.
- Implement expression evaluation.
- Implement instruction evaluation for `Set` and `Call`.
- Implement statement/block evaluation for `Return`, `If`, `Loop`,
  `Break`, `Continue`, and `Block`.
- Reintroduce proof tree types for CIL'.
- Add runtime-error/no-tree cases.

## Runtime Errors To Model

- Uninitialized read.
- Division or modulo by zero.
- Invalid pointer dereference.
- Out-of-bounds array access.
- Missing function body.
- Nonzero `main` return for normal-completion comparisons.
- Fuel exhaustion.

## Validation

- Keep `dune build` green after each small step.
- Keep `dune runtest` green.
- Keep supported examples passing:
  - `examples/cil_small_while.c`
  - `examples/cil_branch_loop.c`
  - `examples/cil_pointer_array_call.c`
- Keep unsupported examples rejecting as expected:
  - `examples/unsupported_cast_implicit.c`
- Add one direct CIL' unit test for every new checker or semantic invariant
  that cannot be expressed as valid C source.

## Analyzer / Synthesis Reconnect

- Re-enable synthesis only after CIL' Big-Step stabilizes.
- Make synthesis generate well-formed CIL' directly.
- Use `Check.check_file` as an optional debug guard, not necessarily on the
  final fast synthesis path.
- Re-enable Sparrow wrapper after printable CIL' programs are stable.
- Check exported C against Sparrow's CIL 1.7.3 frontend before relying on an
  attack result.

## Cleanup

- Keep `.agents/CONTEXT.md` updated with current implementation state.
- Keep `.agents/LANGUAGE.md` as the language/semantics source of truth.
- Do not update `.agents/c-feature.csv` until the feature matrix is
  intentionally revisited.
